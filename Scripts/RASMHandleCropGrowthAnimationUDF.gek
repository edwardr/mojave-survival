scn RASMHandleCropGrowthAnimationUDF

ref slotRef

begin function { slotRef }
	if slotRef.GetIsId RASMCropBrocFlower == 1 || slotRef.GetIsId RASMCropWhiteHorseNettle == 1 || slotRef.GetIsId RASMCropMaize == 1 || slotRef.GetIsId RASMCropXanderRoot == 1
		slotRef.playGroup Forward 1
	else
		slotRef.playGroup Backward 1
	endif
end