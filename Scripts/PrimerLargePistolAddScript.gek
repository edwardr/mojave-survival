scn PrimerLargePistolAddScript

int killme 

BEGIN OnAdd Player
	set killme to 1
END

begin GameMode
;	int countofstuff;
;	int numberToAdd;


	if (killme == 1)
;		set countofstuff to (player.GetItemCount BoxPrimersLargePistol)

;		set numberToAdd to countofstuff*25
		; RASM replace with pistol primer
		Player.AddItem RASMPrimerPistol 25
		;Player.AddItem PrimerLargePistol 25
		RemoveMe
	endif

end