scn RASMStimpakEmptyNeedle
short bonusModifier
short skillRoll
ref effectTarget

begin ScriptEffectStart
	set effectTarget to GetSelf
	if effectTarget.GetIsReference player
		set bonusModifier to ((PlayerREF.getav science / 2) + (PlayerREF.getav survival / 2) + PlayerREF.getav luck) / 2
		set skillRoll to GetRandomPercent
		if skillRoll <= bonusModifier
			PlayerREF.additem SurgicalSyringe01 1
		endif
	endif
end